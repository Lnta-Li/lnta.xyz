我想在网站中创建一个api接口服务，此服务可以用于用户端在comfyui中创建一个自定义节点来将用户本地的图片上传到此网站服务器的../RelayStation/image/目录中，并且给用户返回一个图片的url地址。
1.此服务是我本人使用，因此不需要考虑复杂的功能和安全考虑，直接生成10个可用的api密钥储存在../RelayStation/readme.me文件里；
2.设计一种可以用于comfyui中的方法（可能是python？我是小白不是很清楚），将用户传入的图片通过此api上传到网站的../RelayStation/image/目录中，并且给用户返回一个图片的url地址（www.lnta.xyz//RelayStation/image/文件名）；
3.每次用户上传图片时，都将/RelayStation/image/目录内之前的文件清空，然后再保存新文件，只保留最新上传的文件；
3.我是小白，所以不清楚我说的功能能否实现，比如api的方式能不能实现图片的上传，如果不能有什么其他方式能让我实现在comfyui里创建一个自定义节点，将本地图片转换成一个可访问的url；还有很多其他方面比如comfyui的自定义节点是不是python我也不懂，我说的不一定对，请你先仔细分析我的需求，然后思考一下如何才能最方便的实现，且不要出现问题。权衡好然后再执行。